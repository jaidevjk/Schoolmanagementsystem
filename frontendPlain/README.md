This folder contains a static (plain HTML/CSS/JS) version of the React client located at `school-mern-app/client`.

Notes:
- This is a static, frontend-only copy (no backend integrations).
- Files are simplified and intended for local static preview only.
- Keep `frontendPlain` separate; do not modify `school-mern-app` as requested.

Structure:
- index.html (entry)
- css/style.css
- js/main.js
- pages/admin/*, pages/teacher/*, pages/student/*

You can open `index.html` in a browser to preview.
